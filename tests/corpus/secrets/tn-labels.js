// TRUE NEGATIVE — documentation/labels, not real credentials.
// The key NAME is referenced and an example token is clearly a placeholder.
const config = { apiKeyField: "apiKey" };
const exampleToken = "YOUR_API_TOKEN_HERE";
console.log("set apiKey to your real token, e.g. sample-placeholder-value");
